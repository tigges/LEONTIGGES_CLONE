# AGENTS.md

## Cursor Cloud specific instructions

This repository (`LEONTIGGES_CLONE`) is currently an empty/blank repository with only a `README.md` file and a single initial commit. There is no application code, no services, no dependencies, and nothing to build, lint, test, or run.

- **No package manager lockfiles or dependency manifests** exist (no `package.json`, `requirements.txt`, `go.mod`, etc.).
- **No build system** is configured (no `Makefile`, `Dockerfile`, `docker-compose.yml`, etc.).
- **No CI/CD** pipeline is set up.
- **No dev server or application** to start.

When application code is added to this repository, this section should be updated with:
- How to install dependencies (update script)
- How to run lint, tests, and the dev server
- Any non-obvious caveats discovered during setup
